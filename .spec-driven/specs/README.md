# Specs

This directory contains current-state specifications for the project.

Specs describe **observable behavior**, not implementation details. They serve as the source of truth that `spec-driven-verify` checks against.

## Conventions

- One file per domain area or subsystem
- Use present tense: "The API returns...", "Users can..."
- Keep specs stable; changes go through the `changes/` workflow
