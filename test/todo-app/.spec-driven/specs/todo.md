# Todo App Spec

- Users can add a todo item with `node todo.js add <text>`
- Users can list all todos with `node todo.js list`
- Users can mark a todo done with `node todo.js done <n>`
- Todos persist across runs via todos.json
